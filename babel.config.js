module.exports = {
    presets: [
      '@babel/preset-env',      // Transpile latest JS
      '@babel/preset-react',    // JSX support
      '@babel/preset-typescript' // TS support (even if ts-jest handles most)
    ],
  };
  